class Players {

    // List<Player> _playerList;

    // MEMBER VARIABLES / ATTRIBUTES

    // MEMBER METHODS / FUNCTIONS / BEHAVIORS

        // CONSTRUCTORS ( METHODS )

        // GETTERS / ACCESSORS ( METHODS )

        // SETTERS / MUTATORS ( METHODS )

        // OTHER METHODS

    // public void GetPlayers(string header, int min, int max) {
    //     List<List<string>> options = new List<List<string>>();
    //     List<List<string>> playerInfoLoLoS = new List<List<string>>();

    //     if (playerInfoLoLoS.Count < max) {
    //         options.Add(["Add Player"]);
    //     }
    //     if (playerInfoLoLoS.Count > 0) {
    //         options.Add(["Drop a Player"]);
    //         options.Add(["Drop All Players"]);
    //     }
    //     options.Add(["Cancel"]);
        
    //     MultipleChoice();
    // }

}

// class Player {

//     private string _memberVar;

// }
